<h3 class="text-center">会員一覧</h3>
	<table class="table customers-index">
		<thead class="thead-light">
			<th>ID</th>
			<th>氏名</th>
			<th>在住</th>
		</thead>

	<tbody>
		<% @users.each do |user| %>
		<tr>
			<td><%= user.id %></td>
			<td><%= link_to user.name, user_path(user) , length: 10 %></td>
			<td><%= user.prefecture_code %></td>
		<% end %>
		</tr>
	</tbody>
	</table>

<%= paginate @users %>
